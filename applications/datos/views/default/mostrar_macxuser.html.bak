{{extend 'layout.html'}}
<h1>{{=auth.user.first_name}} {{=auth.user.last_name}}</h1>
<h1>{{=machine.ip_machine}} {{=T('of')}} {{=course.name_course}}</h1>

<h2>{{=T('Select one group')}}</h2>

{{for code_machine in c_machine:}}
<div class="well">
        <table style="width:80%">
            <tr><th>Machine</th><th>Grupo</th></tr>
            <tr><td><strong><span class="votes">{{=A(code_machine.ip_machine, _href=URL('show_ports', args=(code_machine.id, code_group.cod_group)))}}</strong></td></tr>
        </table>
</div>
{{pass}}
