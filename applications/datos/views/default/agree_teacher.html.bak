{{extend 'layout.html'}}
<h2>
    Aprobacion Registro Docente
</h2>
<table class="table">
    {{for r in rows:}}
    <tr>
        <td><a href="{{=URL('show_teacher',args=r.id)}}">{{=r.first_name}} {{=r.last_name}}</a></td>

    </tr>
    {{pass}}
</table>
