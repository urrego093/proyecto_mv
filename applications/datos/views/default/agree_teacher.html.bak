{{extend 'layout.html'}}
<h2>
    Registrar Docente
</h2>
<table class="table">
    {{for r in rows:}}
    <tr>
        <td><a href="{{=URL('show_teacher',args=r.id)}}">{{=r.first_name}}</a></td>

    </tr>
    {{pass}}
</table>
