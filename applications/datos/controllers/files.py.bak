# -*- coding: utf-8 -*-
# intente algo como
def clean_machines(): 
    ids = request.vars["ids"]
    redirect(URL('maquinas', 'ejecutar', vars= dict(ids=ids, opcion='clean')))
    return dict(message="hello from files.py")
